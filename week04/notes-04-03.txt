<h1>JavaScript Objects</h1>

<table id="sections">
  <caption id="courseName"></caption>
  <thead>
    <tr>
      <th>Section</th>
      <th>Enrollment</th>
      <th>Instructor</th>
    </tr>
  </thead>
  <tbody></tbody>
</table>

let aCourse = {
  code: "WDD131",
  title: "Dynamic Web Fundamentals",
  credits: 2,
  sections: [
    {section: "001", enrolled: 95, instructor: "Roberto Diaz Rodriguez" },
    {section: "002", enrolled: 80, instructor: "Sarah Gobble" }
  ]
};

function setCourseInformation(course) {
  document.querySelector("#courseName").innerHTML = `${course.code} - ${course.title}`
}

function renderSections(course) {
  const tbody = document.querySelector("#sections tbody");
  let rows = "";
  for (const section of course.sections) {
    rows += `<tr>
    <td>${section.section}</td>
    <td>${section.enrolled}</td>
    <td>${section.instructor}</td>
    </tr>`;
  }
  tbody.innerHTML = rows;
}

setCourseInformation(aCourse);
renderSections(aCourse);

Ok, so I am adding this as notes because it was kind of confusing and I am about 100% certain that this class failed to teach me the basic principles
well enough for me to not have been confused. So I have copied the html and javascript of the practice to try to have somewhere the stuff I am needing to learn.

First is let aCourse. That is an object, similar to what I learned in cse 210. However, I also need to note it works like a dictionary in python. The key value is the thing before the colon,
for example, code is a key. its matching value that will get pulled up is WDD131.

inside of this object, you can have arrays which also have the dictionary treatment. for example, the sections bit. In sections, there are two values, each comprised of three groupings. the
left of the colon is the key and the right is the value.

I think the biggest brain fart is the function that is a foreach loop. Although it is a logical conclusion that you can make a table and stuff, that never would have naturally occured to me and
I find it very discomforting that I am finding out through lesson activities instead of the lessons. the loop itself is straightforward enough that I don't think I need to specifically explain
it. However, I will note that the querySelector method allows one to grab by id if you put the hashtag symbol in front of it. Also, you can then use a space to specify a specific parent
of the id you are grabbing. In this instance the id sections is matched to a table item in html, which contains a tablebody element, which you are then selecting.

The very bottom two lines of code are calling the functions so that they actually do something instead of just sitting there useless.


